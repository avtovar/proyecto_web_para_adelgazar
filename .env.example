# Base de datos (Vercel Postgres asigna esto automaticamente en produccion)
POSTGRES_URL=postgres://usuario:password@host:5432/database

# Secreto para firmar JWTs (autogenerado si no se define)
JWT_SECRET=tu-secreto-super-seguro

# Puerto local (opcional, default 3000)
PORT=3000
